🎯 Academic Control Center - Project Task & Overview

Target Audience: นักเรียนระดับมัธยมศึกษาตอนปลาย 
Platform: Web Application (Single-Page Application - SPA)

1. Executive Summary (ภาพรวมของโปรเจกต์)

โปรเจกต์ "Academic Control Center" คือระบบจัดการตารางเรียนและตารางชีวิตสำหรับนักเรียนมัธยมปลายที่ต้องเผชิญกับตารางเรียนที่อัดแน่นและภาระงานที่ซับซ้อน โดยระบบนี้ถูกออกแบบมาให้เป็น "Local-First Application" คือทำงานเบ็ดเสร็จบนเบราว์เซอร์ของผู้ใช้โดยไม่จำเป็นต้องพึ่งพา Backend Database (ใช้ LocalStorage ในการเก็บข้อมูล) เพื่อความรวดเร็วและเป็นส่วนตัว

หัวใจหลักของแอปพลิเคชันคือ "ความยืดหยุ่นในการจัดการ (Flexibility)" และ "การมองเห็นภาพรวม (Visual Clarity)" ผ่านระบบการลากวาง (Drag & Drop) และระบบรหัสสี (Color Coding) ที่แบ่งตามระดับความยากและความสำคัญ

2. Core Objectives (เป้าหมายหลัก)

Time Management: ให้นักเรียนสามารถวางแผนตารางเรียนรายสัปดาห์ได้ง่ายที่สุด ผ่านหน้าต่าง (Overlay) แบบ Drag & Drop

Task Tracking: ติดตามการบ้านและโปรเจกต์ผ่านกระดาน Kanban (To Do, Doing, Done) เพื่อไม่ให้ตกหล่น

Visual Hierarchy: ทำให้ผู้ใช้รู้ทันทีว่าวิชาไหนยาก งานไหนด่วน ผ่านระบบป้ายกำกับสี (Red = Hard/Urgent, Amber = Medium, Emerald = Easy)

Frictionless Experience: ระบบต้องลื่นไหล คลิกเพื่อดู Tooltip ได้ทันที ลากวางได้โดยไม่ต้องโหลดหน้าเว็บใหม่

3. User Flow (ลำดับการใช้งานของผู้ใช้)

การทำงานของระบบถูกออกแบบให้ครอบคลุม Use Case จริงของนักเรียน ดังนี้:

Step 1: Authentication & Onboarding

ผู้ใช้เข้าสู่ระบบครั้งแรก จะพบกับหน้า Login/Register (ครอบทับหน้าจอทั้งหมด Z-index: 100)

ผู้ใช้เลือก "ลงทะเบียนที่นี่" พิมพ์ Username และ Password

ระบบบันทึกข้อมูลลง localStorage และสร้าง Mock Data (ข้อมูลจำลองตารางเรียนสายวิทย์-คณิต) ให้อัตโนมัติ เพื่อให้ผู้ใช้เห็นภาพการทำงานทันที

ผู้ใช้เข้าสู่ระบบด้วย Username/Password ที่สมัครไว้

Step 2: Dashboard (หน้าหลัก)

Header: แสดงชื่อผู้ใช้ปัจจุบัน และปุ่มเครื่องมือหลัก (จัดการตารางเรียน, คลังงาน, ล็อกเอาท์)

Sidebar (ซ้าย): แสดง "ตารางเรียนวันนี้" ดึงข้อมูลเฉพาะวันที่ตรงกับวันปัจจุบันของระบบ (Today) มาแสดงเรียงตามเวลา

Kanban Board (ขวา): กระดานแสดงงานที่ต้องทำ 3 คอลัมน์ ผู้ใช้สามารถกดปุ่มลูกศร < หรือ > บนการ์ดเพื่อย้ายสถานะงานได้

Step 3: Managing Weekly Schedule (การจัดตารางรายสัปดาห์)

ผู้ใช้กดปุ่ม "จัดการตารางเรียน" ระบบจะเปิด Overlay ขนาดใหญ่ (#weekly-overlay)

Sidebar ของตาราง: แสดง "รายวิชาที่มี" (Subject Pool) ซึ่งเก็บวิชาทั้งหมดที่ผู้ใช้มี (day: '0')

Grid ตาราง (จันทร์-ศุกร์): ช่องว่างสำหรับวางวิชาเรียน

Interaction (Drag & Drop): - หากลากจาก "รายวิชาที่มี" -> ลง Grid: ระบบจะเก็บแม่แบบไว้ที่เดิม และเด้ง Modal ให้ผู้ใช้กรอก "เวลาเรียน" และ "ห้องเรียน" ใหม่สำหรับวันที่ลากไปวาง

หากลากวิชาภายใน Grid ด้วยกันเอง (ย้ายวัน) -> ระบบจะเปลี่ยนวันให้ทันทีโดยไม่เด้ง Modal

Step 4: Managing Tasks (การจัดการงาน)

ผู้ใช้กดปุ่ม Floating Action Button (FAB) รูป + มุมขวาล่าง

ระบบเปิด Modal "เพิ่มงานใหม่" ผู้ใช้สามารถเลือกวิชาจาก Dropdown (ดึงมาจากวิชาที่มีในตาราง) หรือพิมพ์ชื่อวิชาใหม่เองก็ได้

งานจะไปปรากฏในคอลัมน์ "To Do"

เมื่องานย้ายไปที่ "Done" ผู้ใช้สามารถเข้าไปดูใน "คลังงาน (Archive)" เพื่อดูงานที่สำเร็จแล้ว หรือดึงกลับมาแก้ไขได้

4. Current State & Limitations

State: UI และ Business Logic ส่วนของ Frontend เสร็จสมบูรณ์ 100%

Limitation: เนื่องจากใช้ localStorage ข้อมูลจะไม่สามารถซิงค์ข้ามอุปกรณ์ได้ (ผูกติดกับ Browser)

Future Scope: หากมีแผนขยายระบบ ควรเปลี่ยนการเรียก getData() และ saveData() ไปเชื่อมต่อกับ Firebase Firestore หรือ REST API แทน